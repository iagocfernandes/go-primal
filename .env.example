# GO PRIMAL — Production Alpha 0.1
# Public Supabase values for the dedicated go-primal project.
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_SUPABASE_URL=https://csrlvhbmrvblpzogibug.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_QB6Zi43mq1xUX5m_7r8Muw_kv3HJiEF

# Required server-only secret. Get it from Supabase Project Settings > API.
# Never expose this in NEXT_PUBLIC_* variables or commit a real value.
SUPABASE_SERVICE_ROLE_KEY=

# 32-byte base64 key used only on the server to encrypt provider tokens.
# Generate locally: openssl rand -base64 32
INTEGRATION_ENCRYPTION_KEY=

# Strava developer application
STRAVA_CLIENT_ID=
STRAVA_CLIENT_SECRET=
STRAVA_WEBHOOK_VERIFY_TOKEN=

# Secret protecting internal job endpoints / cron requests.
# Generate locally: openssl rand -hex 32
CRON_SECRET=
